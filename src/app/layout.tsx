import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI OS by Flowsulting — Life Operating System",
  description: "The AI-powered operating system for builders. Track habits, ship products, monitor your life metrics — all managed by AI.",
  openGraph: {
    title: "AI OS by Flowsulting",
    description: "The AI-powered operating system for builders. Ship, track, grow.",
    siteName: "AI OS",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
